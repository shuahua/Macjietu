## v1.0.1-build2

# 本次更新

- 区域和窗口截图不再限制用户选区等待时间，仍可按 Esc 取消。
- 修正多显示器坐标转换、跨屏合成和高分辨率裁剪，全屏截图合成整个桌面。
- 长截图增加权限检查和逐屏捕获回退，捕获时隐藏预览浮层，避免重复帧增加高度。
- 文字改为直接在截图上输入；文字工具会话内可调整字体、字号、颜色、字重和下划线，通过边框或 Option 拖动；切换工具后固定，不可重新编辑。
- 改善编辑窗口焦点、参数浮窗初始位置及移动跟随，文字字号独立于画笔粗细。
- 打包递增版本号并附更新说明。

## 已知限制

- 多屏、混合 DPI、中文输入法和真实长截图仍需实机交互验证。
- 长截图全局 Esc 监听仍可能将按键传给前台应用，本版本未解决此问题。
- 自动长截图首帧与滚动的时序、失败后会话清理仍有待完善。
- 使用本地证书签名，未完成 Apple 公证；仅支持 Apple Silicon。

2026-09-11
## v1.0.2-build3

# 本次更新

- 截图导出精简为原始像素（推荐）、2 倍像素、3 倍像素。放大采用插值，不增加底图细节。
- 修正 Retina 图片按点尺寸导出导致的降采样；按源位图像素导出，不受预览缩放影响。
- PNG 编码直接使用源像素，移除 TIFF 中转；兼容旧设置，旧 4/5 倍归并为 3 倍。
- 录屏标清、高清、原画保留，其分辨率或目标码率确有区别。
- 长截图先捕获首帧再滚动，顺序采样，结束时等待在途捕获并补尾帧。
- 长截图隔离过期会话回调，改进失败与取消清理、连续滚动采样。
- 拼接增加可信重叠及歧义校验，避免无可信重叠时硬拼；增加帧数和内存预算检查。

## 已知限制

- 真实多屏、混合 DPI、慢加载页面、动画和触控板惯性仍需实机验收；固定头尾没有自动裁除。
- 长截图全局 Esc 穿透尚未确认解决。
- 超长截图放大仍会增加内存消耗。旧 Retina 用户建议选择原始像素，旧倍率在新规则下可能输出更大图片。
- 本地证书签名，未完成 Apple 公证；仅支持 Apple Silicon。

2026-09-14
## v1.0.3-build4

# 本次更新

- 每张贴图增加图片外侧的关闭按钮，不遮挡图片内容。
- 右键贴图增加“关闭贴图”菜单；贴图获得焦点时支持 Esc 关闭。
- 多个贴图独立关闭，保留拖动能力并改进可视屏幕边界布局。
- 统一关闭清理流程，释放图片、窗口、内容视图及控制器持有引用，重复关闭不会重复回调；应用退出时清理全部贴图。

## 验证与限制

- 新增多贴图独立关闭、关闭入口及资源引用释放回归测试。
- 尚未进行人工桌面交互验收。对象引用释放不代表系统进程内存数值立即下降。
- 保留此前版本已知限制：长截图全局 Esc 穿透尚未确认解决，混合 DPI 和实际滚动页面仍需实机验证。
- Apple Silicon 版本，使用本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.4-build5

# 本次更新

- 移除贴图图片外的关闭按钮及预留空白区域，保留右键“关闭贴图”和 Esc 关闭，多个贴图独立关闭并清理资源引用。
- 文字输入框根据实际排版宽高自适应，输入时扩展、删除时收缩，支持多行和图片边界限制，不再固定占满剩余宽度。
- 矩形、圆形、直线和箭头标注支持通过同风格浮窗调整粗细和颜色；切换工具或取消时清理旧浮窗及颜色面板。

## 验证与限制

- 增加文字框尺寸、形状参数切换及贴图关闭和资源释放回归测试。
- 尚未进行人工桌面交互验收；中文候选窗、拖动手感及多显示器浮窗位置仍需实机验证。
- 保留此前长截图等功能的已知限制，长截图全局 Esc 穿透尚未确认解决。
- Apple Silicon 版本，使用本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.5-build6

# 尚未发布

- 修正标注参数浮窗白边：统一使用透明、无边框窗口和圆角裁剪的毛玻璃内容，保留阴影；调整尺寸时内容覆盖完整边缘。
- 菜单仅显示尚未授予的屏幕录制、辅助功能和麦克风权限提示；已授权项目不再显示，屏幕录制和辅助功能的请求入口也仅在缺失时显示。每次打开菜单刷新权限状态，全部授权后移除权限区及多余分隔线，保留截图、录屏等功能入口。

## 验证与限制

- 回归测试覆盖浮窗透明背景、圆角裁剪、无描边、浅色/深色外观和尺寸变化，以及全部权限组合、连续刷新、功能入口和分隔线。
- 尚未进行人工桌面交互验收；浮窗在真实桌面背景下的白边、浅色/深色视觉效果及系统授权变更后的表现仍需实机验证。
- 保留此前长截图等功能的已知限制，长截图全局 Esc 穿透尚未确认解决。
- 本轮尚未打包、签名或发布；既有发布版本的签名和平台限制见 `CHANGELOG.md`。

2026-09-14
## v1.0.6-build7

# 本次更新

- 修复长截图首帧和采集中丢失手动滚动事件的问题，补齐本地及全局监听、触控板结束和惯性事件处理。
- 手动滚动后自动采样，预览显示累计拼接缩略图，不再只显示最后一帧；停止时补采尾帧。
- 根据图像实际位移判断上下扩展，处理回滚至已覆盖区域。
- 改进稳定内容区域匹配、位移搜索和候选复核，修复固定头尾、重复纹理中唯一锚点被误拒的问题，调整裁剪避免固定页脚混入正文。
- 加强长截图连续会话隔离和完成任务清理；捕获或拼接失败时保留已有可用片段并明确提示，不静默丢图。

## 验证与限制

- 覆盖手动滚动事件、连续会话、失败恢复、上下扩展、累计预览和实际输出像素的回归测试。
- 尚未进行外部应用真实滚轮、触控板和混合 DPI 验收。快速大跨度滚动、页面重排及大面积动画仍可能无法可靠匹配，不会强行拼接。
- 长截图全局 Esc 穿透尚未确认解决。
- Apple Silicon 版本，使用本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.7-build8

# 本次更新

- 手动长截图增加独立定时采样，不再依赖滚轮事件才采图；合并采样请求并限制单个在途任务。
- 自动长截图不再将连续相同帧判为到底；重试后暂停自动滚动并保留手动操作。
- 暂态匹配失败先重采，不立即退出或继续扩大滚动间隙；失败优先交付已验证的部分长图。
- 独立复制选区位图并调整资源预算，避免选区潜在持有整屏图像。
- 增加缩略图完成按钮及菜单完成、取消入口。
- 采用会话拼接计划保存已验证位移和接缝，预览、最终导出及失败恢复不再重新匹配历史边；支持上下扩展及回滚。

## 验证与限制

- 已添加无滚轮监听采样、暂停与恢复、部分图交付、拼接计划及逐像素结果回归测试。
- 尚未进行真实跨应用滚轮、触控板、多屏和动态页面验收；不保证任意页面拼接成功。
- 仍持有完整帧，超长高分辨率截图可能触发资源上限；本次未证明生产内存占用下降。
- 长截图全局 Esc 穿透尚未确认解决。
- Apple Silicon 版本，使用本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.8-build9

# 本次更新

- 长截图采用磁盘帧快照，按需读取，避免长期持有全部原始图片；保留输出尺寸及临时磁盘预算。
- 自动滚动改为小批次连续小步推进，当前画面匹配后再继续，支持滚动中取消。
- 截图时按窗口 ID 与所属进程排除预览及选区边框，不再每次隐藏再显示浮层，减少闪烁。
- 增加磁盘读写错误、窗口过滤失败处理及多帧拼接回归测试。

## 已知限制

- 尚未进行真实页面、多屏及 macOS 13 实机验收，不保证所有页面均能滚动到底。
- 重复内容歧义仍可能拒绝匹配，不强行拼接；超长输出仍有像素和磁盘上限。
- 尚未证明生产进程内存下降或实际滚动丝滑程度；强制终止可能留下临时文件。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.9-build10

# 本次更新

- 设置、截图工具栏和标注参数、长截图控制及预览背景、录屏操作界面统一适度半透明磨砂玻璃风格，强化文字可读性，适配深浅色和减少透明度设置。
- 图片、贴图及视频内容保持原样，不加磨砂遮罩；系统菜单和保存面板保持原生。
- 手动完成长截图时，未确认的尾帧不再触发未完成警告，正常交付此前已验证长图；真实权限、存储和渲染错误仍提示。
- 修正长截图内存预算将不同处理阶段峰值相加的误计费，保留资源保护上限，增加具体预算诊断。
- 优化帧写盘缓冲和失败恢复，避免丢失唯一的磁盘截图快照。

## 验证与限制

- 本次源码全量 86 项测试已通过；打包时另行验证 Release 构建、签名和压缩包。
- 未进行真实桌面视觉和长截图完整交互验收，预算为资源估算，不是进程内存实测。
- 超长输出仍有资源上限，动态页面或重复内容仍可能无法可靠拼接。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.10-build11

# 本次更新

- 修复设置窗口透明标题区域无法拖动的问题，磨砂装饰层不再拦截鼠标，设置控件保持正常交互。
- 降低磨砂材质上方底色遮罩的不透明度，增强半透明效果，同时保留清晰文字及系统减少透明度适配。
- 清除截图画布边框与多重阴影造成的四角多余渲染，修正长图视口和装饰层裁剪；保留原图完整四角和导出像素。
- 修复自动长截图尾部细微像素差异导致误报未完成的问题；已有可信拼接且连续无新增时直接交付长图。
- 长截图真实失败保留简短通知和菜单状态，不再使用长篇模态提示；无法确认的尾部不声称完整。

## 验证与限制

- 本次源码全量 93 项测试已通过；打包时另行验证 Release 构建、签名和压缩包。
- 已进行 AppKit 命中、离屏像素及导出一致性检查；未进行实际鼠标拖动、桌面磨砂视觉和同一网页长截图验收。
- 超长输出仍有资源上限，动态页面或重复内容仍可能无法可靠拼接。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.11-build12

# 本次更新

- 修正设置窗口标题与系统窗口按钮重叠的问题，正文避开标题栏安全区域，保留空白区域拖动和控件操作。
- 调整共享磨砂材质与底色遮罩，增强半透明效果；文字保持清晰，仍遵循系统减少透明度设置。
- 普通截图与长截图展示视口统一使用 11pt 圆角，内层裁剪避免四角漏底。
- 图片展示区增加轻柔黑色阴影：不透明度 16%、模糊半径 10pt；独立外层阴影避免与工具栏连成大片。
- 圆角与阴影仅用于展示，复制和保存保留原图完整像素及四角。

## 验证与限制

- 设置窗口修复后全量 93 项测试通过；图片圆角阴影修改后构建及 3 项窗口装饰回归测试通过。
- 打包另行验证 Release 构建、版本、签名与 ZIP 完整性。
- 尚未进行真实桌面磨砂、阴影观感及鼠标拖动、长图滚动交互验收。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.12-build13

# 本次更新

- 功能层采用 Liquid Glass 设计：macOS 26 使用原生 NSGlassEffectView 与 NSGlassEffectContainerView，旧系统保留兼容磨砂后端。
- 统一截图工具栏、参数面板、长截图与录屏操作区的材质与层级，玻璃不覆盖图片和视频内容。
- 添加按钮悬停、按下、选中与焦点反馈，以及面板淡入；遵循系统减少动态效果和减少透明度设置。
- 修复原生玻璃容器的控件布局与点击命中，保留设置标题安全区域及拖动行为。
- 保留截图展示圆角和轻柔黑色阴影，复制与保存仍输出原始完整像素。

## 验证与限制

- 源码全量 96 项测试通过；正常构建、强制兼容模式构建及相关测试通过。
- 发布构建不强制兼容模式；打包时另行核验版本、签名和 ZIP 完整性。
- 测试宿主为 macOS 14，尚未完成 macOS 26 原生光学效果、动态观感与多屏实机验收。
- macOS 13–15 使用兼容磨砂，不等同于 macOS 26 的原生 Liquid Glass。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.13-build14

# 本次更新

- 修复截图与录屏工具栏的玻璃容器点击坐标转换，保证按钮、分段控件及参数控件正确命中。
- 修复原生玻璃切换兼容材质后控件容器零宽的问题，保留窗口缩放后的正常交互。
- 标注参数与录屏操作面板支持正确的键盘焦点；录屏面板关闭干扰控件的背景拖动。
- 工具栏与录屏操作区增加阴影留白，避免底部裁切；原生玻璃不叠加人工阴影，兼容模式使用单层柔和阴影。
- 录屏开始、取消和完成增加一次性回调保护，停止任务绑定对应会话，避免迟到操作影响其他录制。
- 保留截图圆角轻阴影、原始图片导出像素以及录屏音频和清晰度行为。

## 验证与限制

- 源码构建通过，全量 103 项测试通过；强制兼容模式相关 23 项测试通过。
- 测试覆盖窗口根视图命中与按钮原生事件回调；下拉菜单选择使用程序化验证，未完成系统菜单实机鼠标验收。
- 已确认测试运行系统为 macOS 26.6.2，SDK 为 26.5；此前文档误将测试编译目标当作系统版本，现已纠正。
- 阴影完成布局与层配置检查，尚未完成桌面视觉验收；未录制用户屏幕或采集音频。
- macOS 26 使用原生 Liquid Glass，旧系统使用兼容磨砂。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.14-build15

# 本次更新

- 修复工具栏点击穿透到下层窗口的问题：玻璃功能面板增加极低不透明度的命中底层，保留原生 Liquid Glass 效果。
- 防穿透底层仅覆盖功能面板，面板外部透明留白仍可穿透，不扩大桌面阻挡区域。
- 保留此前截图、录屏工具栏交互、参数面板焦点、会话保护和阴影留白修复。
- 已取消参考图的胶囊工具栏改造，保留此前工具栏样式。

## 验证与限制

- 点击穿透修复后全量 105 项测试通过，强制兼容模式相关 25 项测试通过。
- WindowServer 窗口命中负对照已通过：移除命中底层时落到下层窗口，恢复后命中工具栏；外部透明留白仍命中下层。
- 取消胶囊改造后构建与差异检查通过；打包另行核验 Release 构建、版本、签名及 ZIP 完整性。
- 系统窗口命中验证不等同于真实鼠标菜单操作或桌面视觉验收；未申请或绕过全局事件与屏幕捕获权限。
- macOS 26 使用原生 Liquid Glass，旧系统使用兼容磨砂。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.15-build16

# 本次更新

- 录屏设置和录制控制栏改为单行居中布局，保留音频、清晰度及开始、取消、完成操作。
- 录制预览移除系统标题栏、窗口按钮和标题文字；保留播放控制、Esc 关闭及底部操作。
- 预览底部关闭与保存导出工具栏改为紧凑居中，不再铺满视频宽度。
- 根据影片展示尺寸和旋转信息异步适配预览视口，等比缩放而不裁剪或拉伸，避免窗口比例不匹配造成黑边。
- 截图二级参数面板贴近主工具栏，整组工具控件居中。
- 当前文字工具会话中的选中文字增加右下角拖动缩放手柄，可调整真实字号并同步参数面板；切换工具后的既有固定规则保留。
- 保留工具栏防点击穿透、原生玻璃与兼容材质、截图原始像素导出和录屏会话保护。

## 验证与限制

- 源码构建通过，全量 110 项测试通过；强制兼容模式相关 20 项测试通过。
- 覆盖录屏单行布局、影片比例与旋转、异步关闭竞态及导出字节一致性；WindowServer 探针验证工具栏命中及透明外边距穿透。
- 尚未完成人工桌面视觉、多屏拖动和音轨听感验收；视频自身编码黑边不会被裁除。
- 文字拖拽缩放的真实鼠标体验及混合字号表现仍需实机验证。
- 打包另行核验 Release 构建、版本、签名和 ZIP 完整性。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-14
## v1.0.16-build17

# 本次更新

- 设置窗口采用与工具栏一致的玻璃效果，新增左侧通用、快捷键、关于分类导航，右侧展示具体设置。
- 现有自动复制、开机启动和截图导出尺寸归入通用设置。
- 区域截图、窗口截图、全屏截图、长截图自动滚动、长截图手动滚动、录屏均支持点击后按键录入自定义快捷键。
- 保留已有默认绑定，手动长截图与录屏默认未设置；支持清除绑定并持久保存。
- 录入期间暂停本应用全局快捷键，Escape、失焦或切换分类可取消；重复绑定、注册或保存失败保留旧设置并提示。
- 关于正文：免费截图工具，全部由AI生成，sang。

## 验证与限制

- 源码构建和全量 116 项测试通过，强制兼容模式相关 20 项测试通过。
- 覆盖配置迁移、清除持久化、冲突回滚、六动作分发、录入取消与焦点恢复；设置分类完成 WindowServer 命中检查。
- 尚未人工验收物理键盘录入、第三方快捷键冲突、拖动及磨砂观感；macOS 13 未真机运行。
- 打包另行核验 Release 构建、版本、签名和 ZIP 完整性。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-15
## v1.0.17-build18

# 本次更新

- 状态栏菜单中未绑定快捷键的功能不再显示“未设置”，已绑定快捷键使用菜单快捷键列展示；改绑或清除后同步刷新。
- 设置快捷键录入页保留“未设置”提示，方便配置未绑定功能。
- 设置窗口左侧分类与右侧内容之间新增细竖分隔线，保留上下留白并适配深浅色与减少透明度设置。
- 修正分类按钮原生底形与选中高亮叠加的问题，选中、未选中、悬停及按下状态共用一致圆角边界，保留焦点反馈及动作绑定。
- 保留玻璃功能层防点击穿透、窗口拖动和标题安全区域。

## 验证与限制

- 源码构建通过，全量 122 项测试通过；强制兼容模式相关 25 项测试通过。
- 覆盖六项菜单动作改绑、清除和显示，分类按钮动作、分隔布局、离屏轮廓与状态切换，以及 WindowServer 窗口命中。
- 自动验证不等同于人工视觉、真实鼠标操作与拖动验收。
- 打包另行核验 Release 构建、版本、签名及 ZIP 完整性。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-15
## v1.0.18-build19

# 本次更新

- 新增水滴玻璃选中反馈，设置分类与截图工具标签使用共享高亮，在连续切换时平滑移动。
- 普通操作按钮使用短暂按压反馈，选项和开关保留实际选中状态；保留动作、焦点和禁用行为。
- 快速切换从当前动画位置续接，遵循减少动态效果和减少透明度设置；兼容模式保留高光反馈。
- 保留现有工具栏布局、设置分隔线、防点击穿透及原始图片导出，不包含此前取消的胶囊工具栏改造。

## 验证与限制

- 本轮源码原生全量 132 项测试通过，原生及强制兼容模式相关测试各 38 项通过，包含 10 项新增水滴测试。
- 已进行离屏轮廓与 WindowServer 命中验证；尚未完成人工桌面连续切换视觉及 macOS 13 实机验收。
- 兼容高光不等同于原生光学折射，系统菜单与保存面板保持原生。
- 打包另行验证 Release 构建、版本、签名和 ZIP 完整性。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-15
## v1.0.19-build20

# 本次更新：1.0.19

- 取消水滴选中效果，恢复 1.0.17 build18 的选中交互：设置侧栏使用单一圆角底形，不恢复曾出现的双底形缺陷。
- 恢复普通按钮 hover、pressed、focus 反馈，以及截图工具原生 segmented 选中和形状选中边框；下拉菜单恢复原生控件。
- 保留六项快捷键自定义及菜单无绑定时不显示“未设置”、设置分隔线、录屏布局与比例、文字 resize 和参数面板间距。
- 保留现有工具栏布局、设置分隔线、防点击穿透及原始图片导出，不包含此前取消的胶囊工具栏改造。

## 验证与限制

- 撤销后源码构建通过，全量 122 项测试通过，原生与强制兼容模式相关测试各 40 项通过；此前水滴测试结果不作为本次证据。
- 强制兼容编译不等同于 macOS 13 实机验收；系统菜单与保存面板保持原生。
- 本版递增为 1.0.19/build20，不回退发布版本；安装并重启新版后才会使用恢复后的选中效果。
- 打包另行验证 Release 构建、版本、签名和 ZIP 完整性。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-15
## v1.0.20-build21

# 本次更新

- 清空、复制、保存、贴图四个操作按钮改为瞬时按压反馈：按下时高亮，松开立即恢复，不保留蓝色激活底色。
- 四个操作按钮悬停不再模拟选中，键盘焦点仅保留细描边；移出按钮后松开不执行操作。
- 保留左侧绘图工具的持续选中行为、关闭按钮行为及工具栏防点击穿透修复。

## 验证与限制

- 全量 123 项测试通过；最终限定四图标调整后重新构建及工具栏相关 4 项测试通过。
- 覆盖原生、兼容和不透明后端，以及原生鼠标跟踪、取消、连续点击、键盘触发和释放后状态清理。
- 测试进程验证不等同于用户运行实例的人工视觉验收。
- 打包另行核验 Release 构建、版本、签名与 ZIP 完整性。
- Apple Silicon 版本，本地证书签名，未完成 Apple 公证。

2026-09-15
